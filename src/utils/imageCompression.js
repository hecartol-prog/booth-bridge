/**
 * RC9 compression — delegates to RC10 preprocessing.
 */
export {
  readCompressedImageAsDataUrl,
  preprocessBusinessCardImage,
  preprocessBusinessCardImageLight,
  preprocessBusinessCardImageSafe,
} from "@/utils/imagePreprocessing";
